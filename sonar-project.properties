# todo
#
# create rule profile in external file and link to it
#
# https://github.com/spirit-dev/php-sonarqube-api may be useful?
# use `scm` field to get `blame` functionality ?



# must be unique in a given SonarQube instance
sonar.projectKey=wporg-code-analysis
	# how would we add this for plugins?
	# i guess just have a script to generage it in the downloaded folder
	# or maybe have a single script in the wporg-code-analysis folder, and just change `sonar.source` programmatically
	# or maybe omit it from this file and pass it via cli option


# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=tests

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
